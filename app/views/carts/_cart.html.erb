<% unless cart.line_items.empty? %>
<div class="cart_title" ><h3 style="font-weight: bold; color: violet; font-size: 20px;">Your Cart</h3</div>
<div >..................</div>
<table border="1" style="color: black; font-size: 15px">
	<%= render(cart.line_items) %>
	<tr>
		<td colspan="2">Total</td> 
		<td class="total_cell"><%= number_to_currency(cart.total_price)  %></td>
	</tr>
</table>
<div >..................</div>
<% session[:amount] = cart.total_price%>
<div style="color: blue;>
<%= button_to 'Empty Cart', cart, :method => :delete, :confirm => 'Are you sure?' %>
</div>
<div > </div>
<p>
	<%= link_to "Continue Shopping",:controller=>"gallery",:action=>"index" %> | 

<% end %>

<% if user_signed_in? %>
<%= link_to "Checkout", gallery_checkout_path %>
<% else %>
<%= link_to "Checkout", new_user_session_path %>
<% end %>
